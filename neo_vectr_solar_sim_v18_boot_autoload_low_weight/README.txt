Neo-VECTR's Solar Sim v17

Adds: truth builder controls, merge rules, global integrity diagnostics, split/bundle export, and stricter pack workflow for proven-only data.

Controls remain the same as v16. New controls:
- Merge rule selector in Catalog ingest panel
- Validate all truth
- Export split packs
- Export bundle

This build stays offline and proven-only. It is designed to make larger source-derived truth packs plug in cleanly.
